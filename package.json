{
  "name": "todoapp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "cd server && npm run start",
    "client": "cd client && npm run dev",
    "all": "(cd server && npm run start) && (cd client && npm run dev)"
  },
  "dependencies": {
    "express": "^4.18.2",
    "if-env": "^1.0.4",
    "prop-types": "^15.8.1"
  }
}
